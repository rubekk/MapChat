# open the latest chat with popup
# show total active users

# when expanding and minimizing message map is loading again --solved
# when clicking show me marker is repeated --solved
# make responsive --solved

# when clicking on individual message, not every popup is shown --solved
# show messages with date also/ classify them by date --solved
# do something when location cannot be tracked --solved
# active users is not working  --solved

# show active users on map with a different marker font awesome
# locate yourself with a different marker